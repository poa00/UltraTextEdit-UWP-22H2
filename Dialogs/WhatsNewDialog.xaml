<ContentDialog
    x:Class="UltraTextEdit_UWP.Dialogs.WhatsNewDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    PrimaryButtonText="OK"
    PrimaryButtonStyle="{ThemeResource AccentButtonStyle}"
    Title="What's new"
    mc:Ignorable="d">

    <Grid>
        <TextBlock x:Name="ChangelogText" TextWrapping="WrapWholeWords">
            <LineBreak />
            • First ever Canary build of UTE UWP!<LineBreak />
            • The major version is now 11, not 10, and the build strategy has been rebooted to start from 7700. Please note that this will ONLY ever be present in Canary builds or builds based on the new, merged Windows App SDk codebase that is coming soon<LineBreak />
            • Introduces a brand new Ribbon UI design, more heavily inspired by modern Word releases and users' feedback!<LineBreak />
            • Fixes several issues that led to tabbed MainPage have more features (a feature disproportionality) than untabbed MainPage<LineBreak />
        </TextBlock>
    </Grid>
</ContentDialog>